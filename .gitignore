# macOS
.DS_Store

# OMT private material
docs/local/
.omt/

# Node / JS
node_modules/
dist/
dist-ssr/
.eslintcache
*.tsbuildinfo

# electron-builder output. Kept separate from dist/ so packaged apps can't be
# swept back into the next build's asar.
release/
*.dmg
*.dmg.blockmap

# Rust (Tauri)
target/

# Xcode
DerivedData/
*.xcuserstate

# Environment & secrets
.env
.env.local
